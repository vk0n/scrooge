base_backtest_config_path: config/backtest.yaml
compare_run_dir: auto
compare_run_root: runtime/compare
compare_parallel: true
compare_max_workers: 3
base_backtest_overrides:
  backtest_period_days: 30
  backtest_period_end_time: ''
  execution_mode: simulated
scenarios:
  - name: realtime-30d-5s-intrabar
    overrides:
      strategy_mode: realtime
      backtest_input_mode: agg_trade_stream
      indicator_inputs:
        ema: intrabar
        rsi: intrabar
        bb: intrabar
        atr: intrabar
  - name: realtime-30d-5s-closed
    overrides:
      strategy_mode: realtime
      backtest_input_mode: agg_trade_stream
      agg_trade_tick_interval: 5s
      indicator_inputs:
        ema: closed
        rsi: closed
        bb: closed
        atr: closed
  - name: realtime-30d-5s-mixed
    overrides:
      strategy_mode: realtime
      backtest_input_mode: agg_trade_stream
      agg_trade_tick_interval: 5s
      indicator_inputs:
        ema: closed
        rsi: closed
        bb: intrabar
        atr: intrabar
